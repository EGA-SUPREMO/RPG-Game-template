package qef.uzantinterfac;

import java.awt.Color;

import qef.ilj.DebugDesegn;

public class Submenu {
	
	
	
/*	private static int x = 11, y = 11;
	
	public static void desegnBarn(final int alt, final int kvant, final int plejkvant, final int id) {
		
		final int largx = 100;
		final int pocent = largx * kvant / plejkvant;
		
		DebugDesegn.setColor(Color.BLACK);
		DebugDesegn.desegnMargxenRectangle(x * id - 1, y - 1, largx + 1, alt + 1);
		
		DebugDesegn.setColor(Color.CYAN);
		DebugDesegn.desegnRectangle(x * id, y, largx, alt);
		
		DebugDesegn.setColor(Color.ORANGE);
		DebugDesegn.desegnRectangle(x * id, y, pocent, alt);
		
	}*/
	
}